### Iterator

```java
Iterator<Integer> it = v.iterator();
while(it.hasNext()){
    int n = it.next();
    //...
}
```

### 향상된 for 루프

```java
for(String name : students){
    System.out.println(name)
}
```
